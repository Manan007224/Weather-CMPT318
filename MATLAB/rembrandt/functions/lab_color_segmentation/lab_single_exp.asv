clc;
clear;
close all;

FILENAME = 'texture.bmp'
I = imread(FILENAME);
n=5;
L = lab_segmentation(n,I);
figure;


for i=(1:n)
if mod(n,2) ~= 0
    subplot(floor(n-n/2),floor(n/2),i)
else
    subplot(floor(n-n/2),floor(n/2),i)
   emnd 
end
